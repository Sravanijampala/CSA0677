#include <stdio.h>

int main() {
    int containers[5] = {10, 20, 30, 40, 50};
    
    for (int i = 0; i < 5; i++) {
        printf("Container %d: %d units\n", i+1, containers[i]);
    }
    
    return 0;
}
