#include <stdio.h>

int main() {
    int n = 10;
    int sum = 0;

    // O(n) time complexity
    for (int i = 1; i <= n; i++) {
        sum += i;
    }

    // O(1) time complexity
    printf("Sum of first %d natural numbers: %d\n", n, sum);

    return 0;
}
